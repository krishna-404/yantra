TITLE: [Spec] Strip journal fan-out event + journal Novu workflow (OneQ domain)
---
### type

strip-module

### stack

node, pg-tbus, novu

### tier-estimate

T1

### depends-on

#9

### parallel-group

1.B

### Problem

The event bus and Novu layer still carry OneQ journal plumbing:
`journal_entry_created_fanout` task in `apps/backend/src/events/events.schema.ts` and
`apps/backend/src/novu/workflows/journal_entry_created.workflow.ts`. Their producers
died with SB-8/9; the dead wiring now only confuses greps and Novu sync.

### Bet

If we remove the fan-out task and the Novu workflow, `grep -ri journal apps/backend/src`
returns 0 hits, verifiable in CI.

### Success criteria

- [ ] `journal_entry_created_fanout` task def + handler + emit sites removed from `apps/backend/src/events/`.
- [ ] `apps/backend/src/novu/workflows/journal_entry_created.workflow.ts` deleted; `novu/workflows/index.ts` updated.
- [ ] `grep -ri "journal" apps/backend/src --include="*.ts"` → 0 hits.
- [ ] `user_created` / `user_reminder` workflows and all pg-tbus INFRA untouched.
- [ ] NOTHING under `apps/backend/src/modules/sync/` changed (D16).
- [ ] All of: `yarn lint`, `yarn check-types`, `yarn test:run` green in CI.

### Out of scope

Frontend (#11/#12), zod-schemas (#13), migrations (#14), the `novu:sync` deploy step (note in PR body that operator should run `yarn workspace @connected-repo/backend novu:sync` post-merge).

### Evaluation

Post-merge canary green; operator runs novu:sync and confirms the workflow list no longer contains journal_entry_created. Revert condition: user_created/user_reminder notification tests regress.

### Context & pointers

apps/backend/src/events/events.schema.ts:41-60 · apps/backend/src/novu/workflows/ · apps/backend/package.json novu:sync script.

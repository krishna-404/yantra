TITLE: [Spec] Strip backend prompts module (OneQ domain)
---
### type

strip-module

### stack

node, orchid, orpc

### tier-estimate

T1

### depends-on

#8

### parallel-group

1.B

### Problem

`apps/backend/src/modules/prompts/` is OneQ's journaling-prompt domain — second link of
the backend strip chain (B2). It exists only to feed journal entries, which SB-8
removed.

### Bet

If we remove the prompts backend module and its wiring, backend builds and tests green
with zero prompt-domain references, verifiable by CI + greps below.

### Success criteria

- [ ] `apps/backend/src/modules/prompts/` deleted; router/procedure wiring removed.
- [ ] `grep -rin "prompts" apps/backend/src --include="*.ts"` hits only unrelated uses (e.g. LLM prompt strings elsewhere) — list each surviving hit in the PR body with a one-line justification.
- [ ] NOTHING under `apps/backend/src/modules/sync/` changed (D16).
- [ ] Tables/migrations NOT touched (SB-14's scope).
- [ ] All of: `yarn lint`, `yarn check-types`, `yarn test:run` green in CI.

### Out of scope

events/Novu wiring (#10), frontend prompts module (#11/#12 handle frontend), zod-schemas (#13), migrations (#14).

### Evaluation

Post-merge canary green. Revert condition: any sync test regression on staging.

### Context & pointers

apps/backend/src/modules/prompts/ · apps/backend/src/routers/ · SB-8's merged diff (prior art for the wiring pattern).

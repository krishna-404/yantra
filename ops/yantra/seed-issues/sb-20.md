TITLE: [Spec] AGENTS.md refresh: OneQ references out, .brain/ + spec pointers in
---
### type

docs

### stack

docs

### tier-estimate

T1

### depends-on

#16

### parallel-group

1.C

### Problem

AGENTS.md (root + per-app) still documents the OneQ journal domain and predates the
Yantra spec — agents briefed from it get stale architecture and no pointer to
`.brain/` or `docs/yantra/`. It's the machine-facing doc, so staleness here directly
misleads every execute run.

### Bet

If AGENTS.md reflects post-strip reality and points at `.brain/` + the Yantra spec,
agents stop citing removed modules, verifiable by the greps below.

### Success criteria

- [ ] `grep -ri "journal\|oneq\|prompts module\|streak" AGENTS.md apps/*/AGENTS.md packages/*/AGENTS.md` → 0 hits (worker AGENTS.md included).
- [ ] Root AGENTS.md gains a short "Yantra" section: reading order (docs/yantra/00-overview.md §6), `.brain/` role, the rails paths agents must never touch.
- [ ] Per-app AGENTS.md files updated only where they reference removed modules — no wholesale rewrites.
- [ ] Diff ≤ 200 lines total (this is a correction pass, not a rewrite).
- [ ] All of: `yarn lint`, `yarn check-types`, `yarn test:run` green in CI.

### Out of scope

README (SB-1 handled its claims), conventions distillation (#16 owns .brain/conventions.md).

### Evaluation

Next 5 execute runs: no PR cites a removed module path. Revert condition: none plausible (docs-only).

### Context & pointers

AGENTS.md · apps/backend/AGENTS.md · apps/frontend/AGENTS.md · apps/frontend/src/worker/AGENTS.md · packages/*/AGENTS.md · .brain/ (#16).

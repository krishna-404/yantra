TITLE: [Spec] Seeds/fixtures cleanup + OneQ phase-level verification greps (B8+B9)
---
### type

strip-module

### stack

node, orchid

### tier-estimate

T1

### depends-on

#14

### parallel-group

1.B

### Problem

Seeds and test fixtures still create OneQ demo data (journal entries, prompts, streaks)
against tables that no longer exist after #14 — and the phase needs its final
verification pass (B9): proof the whole strip held without collateral damage.

### Bet

If we purge OneQ from seeds/fixtures and run the phase-level acceptance greps, the
calibration strip is provably complete, verifiable by the exact greps below in CI
output pasted to the PR body.

### Success criteria

- [ ] Seed files (`apps/backend/src/db/seed/`) and test fixtures create no journal/prompt/streak data; `yarn db seed` green on fresh DB.
- [ ] Phase acceptance: `grep -ri "journal\|oneq\|streak" apps packages --include="*.ts" --include="*.tsx" | grep -v docs/` → 0 hits (paste full command output in PR body).
- [ ] Fresh-DB cycle green: `yarn db create && yarn db up && yarn test:db:setup && yarn test:run` (CI proves it).
- [ ] Sync-engine test suite untouched and green (D16 final check — name the suite files in the PR body).
- [ ] All of: `yarn lint`, `yarn check-types`, `yarn test:run` green in CI.

### Out of scope

Any further refactors; this closes group 1.B.

### Evaluation

Post-merge canary green; nightly dream's repo scan finds no journal hits. Revert condition: seed failure on fresh DB.

### Context & pointers

apps/backend/src/db/seed/ · packages/zod-schemas (fixtures removed in #13) · docs/yantra/02-phase-1-calibration.md B8/B9 + phase-level acceptance.

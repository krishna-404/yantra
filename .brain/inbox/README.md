# Inbox — pre-curation lesson stubs

Dream's per-turn candidate lessons land here as raw stubs; the nightly consolidation run
curates them (via PR) into `decisions.md`, `conventions.md`, or `negative-knowledge.md`.
